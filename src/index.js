const me = require('./me');
const my = require('./my');
const contacts = require('./contacts');
const rooms = require('./rooms');
const incomingRequests = require('./incomingRequests');

module.exports = { me, my, contacts, rooms, incomingRequests };
